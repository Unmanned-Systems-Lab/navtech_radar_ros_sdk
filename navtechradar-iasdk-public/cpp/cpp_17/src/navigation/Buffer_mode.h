#ifndef BUFFER_MODE_H
#define BUFFER_MODE_H

namespace Navtech::Navigation {

    // Buffer modes are really only useful with a staring radar
    //
    enum class Buffer_mode { off, average, max };
    
} // namespace Navtech::Navigation
#endif